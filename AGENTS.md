# Agent Notes

- The product/app is called Vidwright. Do not refer to it as Vidwright in user-facing text, MCP metadata, docs, or chat.
- `vidwright` may still appear as a legacy package name, protocol/file-extension namespace, bridge identifier, repository URL, or backward-compatible MCP alias. Treat those as internal compatibility identifiers only.
